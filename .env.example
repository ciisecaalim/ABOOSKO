# Copy to .env.local and enter your PostgreSQL password.
# URL-encode special characters in the password (for example, @ becomes %40).
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@127.0.0.1:5432/app_db
